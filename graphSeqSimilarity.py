#!/usr/bin/python

import DAG

def readGraphSeq(filename):
    Graphs = []
    l = []
    for line in open(filename):
        arg = line.strip("\n")
        l.append(l)
    for line in l:
        if line.startswith("t"):
        else if:
        else:


readGraphSeq("graph.log")
